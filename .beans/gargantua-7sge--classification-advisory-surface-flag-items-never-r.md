---
# gargantua-7sge
title: Classification Advisory surface (flag items; never rewrite safety)
status: completed
type: task
priority: normal
created_at: 2026-04-20T14:06:22Z
updated_at: 2026-04-20T21:47:23Z
parent: gargantua-8igf
---

Add an advisory pass that reviews `review` (🟡) scan results and *suggests*
the user might want to reclassify, without ever changing the YAML-assigned
safety level. PRD §2.5 and §6.2 are explicit that AI is advisory-only.

## Scope

- New surface on `LocalAIService` (or a small sibling service that owns
  this) along the lines of:

  ```swift
  func advisory(for results: [ScanResult], rules: [String: ScanRule])
      async throws -> [ScanResultAdvisory]
  ```

  where `ScanResultAdvisory` carries the result id, a short text
  rationale, and the AI's *suggested* alternative safety level — never
  applied automatically.
- Drive the advisory through the same `AIInferenceEngine` boundary so it
  benefits automatically when MLX lands. Template engine produces a
  structured "review this" suggestion from rule metadata; real engine
  produces something grounded in file path / source.
- Caller surface: a new UI row or toolbar action on review-tier items
  that shows the advisory when the user asks. Do not auto-display.

## Safety invariant

- Advisory must never mutate `ScanResult.safety` or any persisted model
  record. A unit test pins this invariant.

## Out of scope

- UI polish / visual design — functional stub is fine; design review
  happens separately.
- Writing advisories for `safe` or `protected` items; scope is `review`
  only for v1.

## Acceptance

- [x] `advisory(for:rules:)` returns well-formed `ScanResultAdvisory`
      values for review-tier inputs
- [x] Test: advisory invocation does not mutate input `ScanResult.safety`
- [x] Falls back to YAML rule text on engine failure (same pattern as
      `explain`)

## Completed

**Files changed:**
Sources/GargantuaCore/Models/ScanResultAdvisory.swift (new), Sources/GargantuaCore/Services/AIInferenceEngine.swift (modified), Sources/GargantuaCore/Services/LocalAIService.swift (modified), Tests/GargantuaCoreTests/Services/LocalAIServiceAdvisoryTests.swift (new)

**Key decisions:**
Filter to review-tier only for v1 scope. Extended AIInferenceEngine protocol with advisory(for:rule:) + default impl that wraps generate() so Template/MLX engines inherit reasonable behavior without per-engine code. Duplicated lifecycle/idle-timer block from explain() rather than refactor — ambient-refactor risk flagged in memory. Safety invariant enforced by Swift value semantics + pinned by unit test. Per-item YAML fallback on engine throw; full-batch fallback on unavailable model. Missing rule for a result silently skipped.

**Notes for next task:**
API: LocalAIService.advisory(for:rules:) where rules is keyed by ScanResult.id. UI hook/trigger is deferred as out-of-scope (file as follow-up bean if a Review-tier toolbar action is wanted). When implementing an MLX advisory, override AIInferenceEngine.advisory to parse a structured model response rather than relying on the default that ties suggestedSafety to result.safety. ScanResultAdvisory{resultId, rationale, suggestedSafety, source} — source is .ai or .rule per-item.

---
*Auto-generated by PASIV*



**Merged in commit:** ffcd90d
