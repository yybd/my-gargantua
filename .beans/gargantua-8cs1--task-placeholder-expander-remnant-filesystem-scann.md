---
# gargantua-8cs1
title: 'Task: Placeholder expander + remnant filesystem scanner'
status: completed
type: task
priority: high
created_at: 2026-04-17T21:49:55Z
updated_at: 2026-04-18T00:14:11Z
parent: gargantua-j8a1
blocked_by:
    - gargantua-anqg
    - gargantua-4xkj
---

Expand {bundleID}/{appName}/{teamID} placeholders in RemnantRule.pathTemplates against a concrete AppInfo, then scan the filesystem and emit RemnantItem instances grouped into an UninstallPlan.

Scope:
- Template expander with escaping rules for spaces, special chars in appName
- Tilde expansion + glob (**) matching consistent with ScanRule behaviour
- Apply RemnantRule.appliesTo scoping per app
- Apply per-rule exclude patterns
- Populate RemnantItem: size, lastAccessed, ruleID, source (resolve {appName} in source.name)
- Return UninstallPlan with optional appBundle + remnants + totalBytes
- Tests: template expansion matrix, scoping, glob semantics, missing-path graceful handling

Blocked by gargantua-anqg (loader) and gargantua-4xkj (app scanner).

## Completed

**Files changed:**
Sources/GargantuaCore/Services/RemnantScanner.swift (new), Tests/GargantuaCoreTests/Services/RemnantScannerTests.swift (new)

**Key decisions:**
Added RemnantScanner as the plan builder for Smart Uninstaller rules. It expands {bundleID}, {appName}, and {teamID} templates against AppInfo, skips team-scoped templates when no team identifier is available, reuses PathExpander for glob semantics, applies AppScope and excludes, deduplicates matched paths, and emits UninstallPlan with an optional app bundle RemnantItem.

**Notes for next task:**
Use RemnantScanner.loadDefaults() for bundled uninstall_rules, then call plan(for:includeAppBundle:) with an AppInfo from AppScanner. Remnant source attribution resolves {appName} and defaults bundleID to the owning app bundle ID. Tests cover placeholder expansion, scope, glob, excludes, missing paths, source resolution, and app bundle inclusion.

---
*Auto-generated by PASIV*
